---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: sopssecrets.sops.stuttgart-things.com
spec:
  group: sops.stuttgart-things.com
  names:
    kind: SopsSecret
    listKind: SopsSecretList
    plural: sopssecrets
    singular: sopssecret
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.source.repositoryRef.name
      name: Source
      type: string
    - jsonPath: .spec.source.path
      name: Path
      type: string
    - jsonPath: .status.conditions[?(@.type=="Applied")].status
      name: Applied
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SopsSecret decrypts a SOPS-encrypted flat key/value file from a GitRepository
          and maps selected keys into a target Kubernetes Secret.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              SopsSecretSpec describes a mapping from a SOPS-encrypted flat key/value
              YAML file into a k8s Secret.
            properties:
              data:
                items:
                  description: |-
                    DataMapping maps one key in the decrypted flat YAML to one key in the
                    target Secret's data.
                  properties:
                    from:
                      description: From is the top-level key in the decrypted flat
                        YAML file.
                      minLength: 1
                      type: string
                    key:
                      description: Key is the target k8s Secret data key.
                      minLength: 1
                      type: string
                  required:
                  - from
                  - key
                  type: object
                minItems: 1
                type: array
              decryption:
                description: DecryptionSpec configures how the source file is decrypted.
                properties:
                  keyRef:
                    description: SecretKeyRef references a key within a Secret in
                      the same namespace.
                    properties:
                      key:
                        minLength: 1
                        type: string
                      name:
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - keyRef
                type: object
              source:
                description: SourceRef identifies a file in a GitRepository.
                properties:
                  path:
                    description: Path within the repository.
                    minLength: 1
                    type: string
                  repositoryRef:
                    description: LocalObjectReference is a reference to an object
                      in the same namespace.
                    properties:
                      name:
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                required:
                - path
                - repositoryRef
                type: object
              target:
                description: MappingTarget describes the target k8s Secret for a SopsSecret.
                properties:
                  adopt:
                    description: |-
                      When true, adopt a pre-existing Secret that is not already
                      managed by this operator. Defaults to false (refuse).
                    type: boolean
                  name:
                    description: Defaults to metadata.name.
                    type: string
                  namespace:
                    description: Defaults to metadata.namespace.
                    type: string
                  type:
                    description: Defaults to Opaque.
                    type: string
                type: object
            required:
            - data
            - decryption
            - source
            type: object
          status:
            description: SopsSecretStatus is the observed state of the SopsSecret.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastAppliedHash:
                description: |-
                  LastAppliedHash is the SHA-256 of the most recently applied target
                  Secret data (deterministic over the key-sorted key/value pairs).
                type: string
              lastSyncedCommit:
                description: LastSyncedCommit is the source repository commit at the
                  last apply.
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation most recently
                  reconciled.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.source.sourceRef.name
      name: Source
      type: string
    - jsonPath: .spec.source.sourceRef.kind
      name: Kind
      type: string
    - jsonPath: .spec.source.path
      name: Path
      type: string
    - jsonPath: .status.conditions[?(@.type=="Applied")].status
      name: Applied
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          SopsSecret decrypts a SOPS-encrypted flat key/value file and maps selected
          keys into a target Kubernetes Secret.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              SopsSecretSpec describes a mapping from a SOPS-encrypted flat key/value
              YAML file into a k8s Secret.
            properties:
              data:
                items:
                  description: |-
                    DataMapping maps one key in the decrypted flat YAML to one key in the
                    target Secret's data.
                  properties:
                    from:
                      minLength: 1
                      type: string
                    key:
                      minLength: 1
                      type: string
                  required:
                  - from
                  - key
                  type: object
                minItems: 1
                type: array
              decryption:
                description: DecryptionSpec configures how the source file is decrypted.
                properties:
                  keyRef:
                    description: SecretKeyRef references a key within a Secret in
                      the same namespace.
                    properties:
                      key:
                        minLength: 1
                        type: string
                      name:
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - keyRef
                type: object
              source:
                description: |-
                  SourceRef identifies a file in a source CR (GitRepository or
                  ObjectSource).

                  Replaces v1alpha1's RepositoryRef-only shape. v1alpha1's
                  source.repositoryRef.name is converted lossslessly into
                  source.sourceRef{Kind: "GitRepository", Name}.
                properties:
                  path:
                    description: Path within the source (git repo path or object key).
                    minLength: 1
                    type: string
                  sourceRef:
                    description: |-
                      SourceKindRef identifies a source CR by kind and name within the CR's
                      namespace. This replaces v1alpha1's RepositoryRef (which only ever
                      pointed at a GitRepository).
                    properties:
                      kind:
                        description: SourceKind identifies the kind of a source referenced
                          by sourceRef.
                        enum:
                        - GitRepository
                        - ObjectSource
                        type: string
                      name:
                        minLength: 1
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                required:
                - path
                - sourceRef
                type: object
              target:
                description: MappingTarget describes the target k8s Secret for a SopsSecret.
                properties:
                  adopt:
                    type: boolean
                  name:
                    type: string
                  namespace:
                    type: string
                  type:
                    type: string
                type: object
            required:
            - data
            - decryption
            - source
            type: object
          status:
            description: SopsSecretStatus is the observed state of the SopsSecret.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastAppliedHash:
                type: string
              lastSyncedCommit:
                type: string
              observedGeneration:
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
    subresources:
      status: {}
